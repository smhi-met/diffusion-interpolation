_experiment_name: "check-configs
_run_name: "test_${now:%Y%m%d_%H%M%S}"
_work_dir: ${oc.env:PWD}/_work/${_experiment_name}/${_run_name}

defaults:
  - callbacks: default
  - logger: default
  - _self_


model:
  some_value: 42
